# 기본은 모두 text, 레포 안에는 LF로 저장
* text=auto eol=lf

# 쉘 스크립트는 LF 강제
*.sh text eol=lf

# Windows 배치 파일은 CRLF 강제 (필요할 때만)
*.bat text eol=crlf
*.cmd text eol=crlf

# 확실히 바이너리인 것들
*.png binary
*.jpg binary
*.zip binary
*.exe binary